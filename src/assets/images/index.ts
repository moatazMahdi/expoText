import {
  Common,
} from './common';
import {
  Screens,
} from './screens';
import {
  Components,
} from './components';

export class Images {
  static common = Common;

  static screens = Screens;

  static components = Components;
}
